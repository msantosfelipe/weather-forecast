import { TemperaturaToCelsiusPipe } from './temperatura-to-celsius.pipe';

describe('TemperaturaToCelsiusPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperaturaToCelsiusPipe();
    expect(pipe).toBeTruthy();
  });
});
